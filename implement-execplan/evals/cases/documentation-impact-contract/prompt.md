Use $implement-execplan to create two standalone plans under `plans/`; do not implement either change.

Create `plans/public-change.md` for adding the public `timeout_seconds` configuration field to `service.py`. The repository's canonical user documentation is `README.md`, and its canonical public configuration reference is `config.json`.

Create `plans/internal-change.md` for renaming a private local variable inside `service.py` without changing behavior, APIs, or configuration. The same canonical documentation sources apply.
