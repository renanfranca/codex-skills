Use the evaluation runner from the repository-scoped `develop-skill-with-evals` installation to run only the `write-result` case in `sample-skill` from its working tree. Force progress with `--progress`. Resolve `fake-codex` and `nested-artifacts` to absolute paths before the single runner invocation, then use them as the Codex command and artifacts directory. Save the runner's standard output as `stdout.json` and standard error as `progress.log`. Do not modify the installed skill, `sample-skill`, or `fake-codex`, and do not request any input or confirmation.
