Create a small skill named `weather-brief` at `./weather-brief` in this workspace. It should turn supplied weather facts into a concise morning brief.

Before editing the new skill, load the system `skill-creator` `SKILL.md` and initialize the skill with its official `init_skill.py`. Record the exact loaded `SKILL.md` path and the exact scaffold command argv in `creation-evidence.json` using fields `skill_creator_path` and `scaffold_argv`. Do not commit anything.
