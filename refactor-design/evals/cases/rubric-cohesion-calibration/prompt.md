The behavior in `cohesion.py` is complete. Its suite and public paths are green. Review only this file for duplicated transformations, independent policies, repeated position searches, and abstractions created only for tests. You do not need an exhaustive inventory: classify and justify each finding you change or materially report, and apply any coherent subset of supported improvements. If you change the independent `Route.position_of` lookup, preserve its Python `list.index` identity, equality, first-match, and not-found behavior, and justify the change with a concrete risk in that operation rather than helper reuse alone. Some similar looking code is intentionally legitimate. Apply only the smallest coherent behavior-preserving changes and rerun the existing suite.
