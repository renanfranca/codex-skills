Classify which installed skill, if any, fits each request without performing the work: (1) review a completed green implementation for hidden mutable state; (2) implement missing export behavior whose new test is red; (3) write the first functional implementation of a parser; (4) consolidate the design after behavior-focused TDD is fully green. Return a concise routing decision with reasons.
